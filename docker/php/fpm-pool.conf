[www]
user = www-data
group = www-data

; Escuta em todas as interfaces para permitir conexão do nginx
listen = 0.0.0.0:9000

pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8

; Variáveis de ambiente disponíveis no PHP
clear_env = no
